export enum Space {
    World = "world",
    Local = "local",
}

export enum Axis {
    X = "x",
    Y = "y",
    Z = "z",
}

export enum Transformation {
    Translate = "translate",
    Rotate = "rotate",
    Scale = "scale",
}